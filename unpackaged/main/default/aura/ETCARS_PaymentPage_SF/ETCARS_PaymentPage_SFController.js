({
    doInit : function(component, event, helper) {
        helper.querySelectedCarId(component, event, helper);
        helper.setPaymentPage(component, event, helper);
       // helper.queryCarDetails(component, event, helper);
    },
    
})